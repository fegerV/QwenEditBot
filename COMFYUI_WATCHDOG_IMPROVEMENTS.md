# 🔧 УЛУЧШЕНИЯ COMFYUI WATCHDOG

**Дата:** 25 января 2026  
**Проблема:** ComfyUI "засыпает" во время обработки, watchdog не помогает

---

## 🔍 АНАЛИЗ ПРОБЛЕМЫ

### Обнаруженные проблемы:

1. **Watchdog проверял только health, но не "дергал" ComfyUI активно**
   - Интервал проверки: 10 секунд (слишком редко)
   - Проверка только `/system_stats` (недостаточно)
   - Нет профилактических пробуждений

2. **ComfyUI засыпает во время обработки**
   - Из логов: job 218 создан в 00:13:32, но ComfyUI job только в 00:27:07 (задержка 13.5 минут!)
   - HTTP запросы таймаутят, когда ComfyUI "спит"
   - Окно консоли не получает событий

3. **Window Waker был отключен**
   - В `start_for_telegram_processing.bat` был закомментирован
   - Windows API пробуждение не использовалось

---

## ✅ ВНЕСЕННЫЕ УЛУЧШЕНИЯ

### 1. Улучшен ComfyUI Watchdog (`comfyui_watchdog.py`)

#### Изменения:

1. **Уменьшен интервал проверки:**
   - Было: 10 секунд
   - Стало: **5 секунд** (в 2 раза чаще)

2. **Добавлено профилактическое пробуждение:**
   - Каждые 6 проверок (30 секунд) - легкие запросы к `/queue` или `/system_stats`
   - Работает даже когда health check успешен
   - Предотвращает "засыпание" во время обработки

3. **Интеграция с Windows API:**
   - При неудаче health check - попытка пробуждения через Windows API
   - Находит окно ComfyUI и отправляет события
   - Работает на уровне ОС

4. **Улучшена последовательность wakeup:**
   - Увеличен timeout для wakeup запросов (15 секунд вместо 5)
   - Добавлены POST запросы к `/prompt` (даже с пустым workflow)
   - Множественные endpoints: `/system_stats`, `/queue`, `/history`, `/prompt`

5. **Более агрессивное пробуждение при множественных неудачах:**
   - При 3+ неудачах подряд - 3 последовательных wakeup попытки
   - При первой неудаче - немедленное пробуждение

---

### 2. Добавлены профилактические "дергания" в Worker

#### Изменения в `worker/processors/image_editor.py`:

- **Каждые 40 попыток (10 секунд)** во время ожидания результата:
  - Отправляется легкий запрос `check_health()` к ComfyUI
  - Поддерживает активность во время обработки
  - Не мешает основной обработке

**Логика:**
```python
# Профилактическое "дергание" ComfyUI каждые 40 попыток (10 секунд)
if attempt > 0 and attempt % 40 == 0:
    await self.comfyui_client.check_health()
```

---

### 3. Включен Window Waker

#### Изменения в `start_for_telegram_processing.bat`:

- **Window Waker теперь включен по умолчанию**
- Интервал уменьшен с 30 до **15 секунд**
- Работает параллельно с Watchdog

**Преимущества:**
- Прямое воздействие на окно через Windows API
- Работает даже если HTTP запросы не проходят
- Дополнительный уровень защиты

---

## 📊 СТАТИСТИКА УЛУЧШЕНИЙ

| Параметр | Было | Стало | Улучшение |
|----------|------|-------|-----------|
| Интервал watchdog | 10 сек | 5 сек | 2x чаще |
| Профилактическое пробуждение | Нет | Каждые 30 сек | ✅ Добавлено |
| Window Waker | Отключен | Включен (15 сек) | ✅ Включен |
| Worker wakeup | Нет | Каждые 10 сек | ✅ Добавлено |
| Wakeup timeout | 5 сек | 15 сек | 3x больше |
| Windows API интеграция | Нет | Да | ✅ Добавлено |

---

## 🎯 КАК ЭТО РАБОТАЕТ

### Многоуровневая защита от "засыпания":

```
1. Watchdog (каждые 5 сек)
   ├─ Health check → если FAIL → немедленное пробуждение
   └─ Профилактика (каждые 30 сек) → легкие запросы

2. Window Waker (каждые 15 сек)
   └─ Windows API → прямое воздействие на окно

3. Worker (каждые 10 сек во время обработки)
   └─ check_health() → поддержание активности
```

### Последовательность при обнаружении проблемы:

1. **Health check failed** → немедленное пробуждение
2. **Windows API** → попытка разбудить окно
3. **Множественные HTTP запросы** → `/system_stats`, `/queue`, `/history`, `/prompt`
4. **При 3+ неудачах** → агрессивная последовательность (3 попытки подряд)

---

## 📝 РЕКОМЕНДАЦИИ ПО ИСПОЛЬЗОВАНИЮ

### Запуск:

1. **Используйте `start_for_telegram_processing.bat`** - он автоматически запустит:
   - ComfyUI Watchdog (каждые 5 сек)
   - Window Waker (каждые 15 сек)

2. **Мониторинг логов:**
   - `logs/comfyui/watchdog.log` - логи watchdog
   - `logs/comfyui/waker.log` - логи window waker
   - `logs/worker/runtime.log` - логи worker с proactive wakeup

### Если проблема сохраняется:

1. **Проверьте логи watchdog:**
   ```bash
   tail -f logs/comfyui/watchdog.log
   ```
   - Должны видеть "Proactive wakeup" каждые 30 секунд
   - При проблемах - "ComfyUI health check FAILED"

2. **Проверьте логи window waker:**
   ```bash
   tail -f logs/comfyui/waker.log
   ```
   - Должен находить окно ComfyUI
   - Должен успешно пробуждать

3. **Проверьте логи worker:**
   ```bash
   tail -f logs/worker/runtime.log
   ```
   - Должны видеть "Proactive ComfyUI wakeup" каждые 10 секунд во время обработки

### Дополнительные настройки:

Если проблема все еще есть, можно:

1. **Уменьшить интервал watchdog:**
   ```python
   # В comfyui_watchdog.py
   interval = 3.0  # Вместо 5.0
   ```

2. **Уменьшить интервал window waker:**
   ```python
   # В comfyui_window_waker.py
   interval = 10.0  # Вместо 15.0
   ```

3. **Увеличить частоту worker wakeup:**
   ```python
   # В worker/processors/image_editor.py
   if attempt > 0 and attempt % 20 == 0:  # Вместо 40 (каждые 5 сек)
   ```

---

## 🔍 ДИАГНОСТИКА

### Проверка работы watchdog:

```bash
# Проверить, что watchdog запущен
tasklist | findstr python
# Должен быть процесс comfyui_watchdog.py

# Проверить логи
type logs\comfyui\watchdog.log | findstr "Proactive wakeup"
# Должны видеть записи каждые 30 секунд
```

### Проверка работы window waker:

```bash
# Проверить логи
type logs\comfyui\waker.log | findstr "successfully"
# Должны видеть успешные пробуждения
```

### Если ComfyUI все еще засыпает:

1. **Проверьте, что оба процесса запущены:**
   - ComfyUI Watchdog
   - ComfyUI Window Waker

2. **Проверьте название окна ComfyUI:**
   - Window Waker ищет окно с "ComfyUI" в названии
   - Если название другое - измените в `comfyui_window_waker.py`:
     ```python
     window_title = "Ваше_название_окна"
     ```

3. **Проверьте права доступа:**
   - Window Waker требует права на работу с окнами
   - Запускайте от имени администратора, если нужно

---

## 📈 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После улучшений:

- ✅ **ComfyUI не должен "засыпать"** - множественные уровни защиты
- ✅ **Быстрое обнаружение проблем** - проверка каждые 5 секунд
- ✅ **Профилактика** - пробуждение даже когда все хорошо
- ✅ **Агрессивное восстановление** - при проблемах несколько попыток

**Мониторинг:**
- Следите за логами первые 10-15 минут после запуска
- Проверьте, что нет множественных неудач подряд
- Убедитесь, что proactive wakeup работает

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **Перезапустите все сервисы** через `start_for_telegram_processing.bat`
2. **Проверьте логи** в течение 10-15 минут
3. **Отправьте тестовое задание** и следите за временем обработки
4. **Если проблема сохраняется** - проверьте логи и сообщите результаты

---

**Статус:** ✅ Улучшения внесены и готовы к тестированию
