# 📊 Руководство по просмотру аналитики

Скрипт `view_analytics.py` позволяет удобно просматривать статистику использования пресетов и промптов пользователями.

---

## 🚀 Быстрый старт

### Полный отчет
```bash
python view_analytics.py
```

Показывает:
- Статистику использования пресетов
- Статистику по пользователям
- Последние джобы

---

## 📋 Опции командной строки

### `--presets`
Показать только статистику использования пресетов:
```bash
python view_analytics.py --presets
```

**Выводит:**
- Общее количество джоб
- Количество джоб с пресетами vs кастомных промптов
- ТОП-20 самых популярных пресетов
- Последние 10 кастомных промптов

### `--users`
Показать только статистику по пользователям:
```bash
python view_analytics.py --users
```

**Выводит:**
- ТОП-20 пользователей по количеству джоб
- Username, User ID, Telegram ID для каждого пользователя

### `--recent`
Показать только последние джобы:
```bash
python view_analytics.py --recent
```

**Выводит:**
- Последние 20 джоб (можно изменить через `--limit`)
- Для каждой джобы: пользователь, пресет (если есть), промпт, статус, дата

### `--days N`
Фильтр по периоду (последние N дней):
```bash
python view_analytics.py --days 7
python view_analytics.py --presets --days 30
python view_analytics.py --users --days 7
```

### `--limit N`
Количество последних джоб (по умолчанию: 20):
```bash
python view_analytics.py --recent --limit 50
```

---

## 📊 Примеры использования

### 1. Статистика пресетов за последнюю неделю
```bash
python view_analytics.py --presets --days 7
```

### 2. ТОП пользователей за месяц
```bash
python view_analytics.py --users --days 30
```

### 3. Последние 50 джоб
```bash
python view_analytics.py --recent --limit 50
```

### 4. Полный отчет за последние 7 дней
```bash
python view_analytics.py --days 7
```

### 5. Только кастомные промпты (через --recent)
```bash
python view_analytics.py --recent --limit 100
```
Затем в выводе ищите строки с маркером `✍️` (кастомный промпт) вместо `🎨` (пресет).

---

## 🔍 Как работает сопоставление пресетов

Скрипт автоматически сопоставляет промпты из джоб с пресетами из базы данных:

1. **Точное совпадение**: Промпт джобы точно совпадает с промптом пресета
2. **Частичное совпадение**: Промпт пресета содержится в промпте джобы

Если промпт не совпадает ни с одним пресетом, он считается **кастомным промптом**.

---

## 📈 Что показывает статистика

### Статистика пресетов:
- **Общее количество джоб**: Все джобы в выбранном периоде
- **С пресетами**: Количество джоб, где промпт совпал с пресетом
- **Кастомных промптов**: Количество джоб с уникальными промптами
- **ТОП пресетов**: Список пресетов, отсортированный по популярности
- **Последние кастомные промпты**: Примеры кастомных промптов с информацией о пользователе

### Статистика пользователей:
- **ТОП пользователей**: Список пользователей, отсортированный по количеству джоб
- Для каждого пользователя: username, User ID, Telegram ID, количество джоб

### Последние джобы:
- **Job ID**: Идентификатор джобы
- **Пользователь**: Username, User ID, Telegram ID
- **Пресет**: Название пресета (если использовался) или "Кастомный промпт"
- **Промпт**: Текст промпта (для кастомных)
- **Статус**: queued, processing, completed, failed
- **Дата**: Время создания джобы

---

## 💡 Полезные советы

1. **Экспорт в файл:**
   ```bash
   python view_analytics.py > analytics_report.txt
   ```

2. **Поиск конкретного пользователя:**
   ```bash
   python view_analytics.py --recent --limit 100 | grep "username"
   ```

3. **Статистика за разные периоды:**
   ```bash
   # За день
   python view_analytics.py --presets --days 1
   
   # За неделю
   python view_analytics.py --presets --days 7
   
   # За месяц
   python view_analytics.py --presets --days 30
   ```

4. **Мониторинг популярности пресетов:**
   ```bash
   # Еженедельный отчет
   python view_analytics.py --presets --days 7
   ```

---

## 🛠️ Технические детали

- **База данных**: Использует SQLite из `backend/qwen.db`
- **Сопоставление**: Автоматическое сопоставление промптов с пресетами
- **Производительность**: Оптимизированные SQL-запросы с индексами
- **Кодировка**: Поддержка UTF-8 для корректного отображения русских символов

---

## ⚠️ Примечания

- Скрипт требует доступ к базе данных `backend/qwen.db`
- Убедитесь, что backend настроен и база данных существует
- Для работы скрипта не требуется запущенный backend (только доступ к БД)

---

**Создано:** 2026-01-25  
**Версия:** 1.0
