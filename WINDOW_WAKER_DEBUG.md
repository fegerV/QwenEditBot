# 🔍 ДИАГНОСТИКА ПОИСКА ОКНА COMFYUI

**Проблема:** Window Waker не находит окно ComfyUI

---

## 📋 ТЕКУЩАЯ ЛОГИКА ПОИСКА

Window Waker ищет окно ComfyUI по следующим критериям:

1. **Процесс:** `cmd.exe`
2. **Класс окна:** `ConsoleWindowClass` или содержит "console"
3. **Исключения:** Окна с названиями, содержащими:
   - `qwen`, `worker`, `bot`, `watchdog`, `waker`, `qweneditbot`
   - `cursor`, `vscode`, `visual studio`, `code`, `editor`

4. **Приоритеты:**
   - **Приоритет 2 (высокий):**
     - Название содержит `comfyui` или `comfy`
     - Название содержит `run_nvidia_gpu` или `run_nvidia`
     - Название содержит `c:\comfyui` или `c:/comfyui`
   
   - **Приоритет 1 (средний):**
     - Пустое название или длина < 10 символов
     - Стандартное cmd.exe окно без признаков наших сервисов

---

## 🔍 ДИАГНОСТИКА

### После перезапуска Window Waker проверьте логи:

```bash
# Логи Window Waker
logs/comfyui/waker.log
```

**Что искать:**

1. **Если окно не найдено:**
   ```
   No ComfyUI window found (cmd.exe console window)
   Found X cmd.exe windows total (all were excluded or didn't match criteria):
     1. 'название окна' (Process: cmd.exe, Class: ConsoleWindowClass, HWND: X)
   ```
   
   Это покажет все найденные cmd.exe окна и почему они не подходят.

2. **Если окно найдено:**
   ```
   Selected ComfyUI window: 'название' (Process: cmd.exe, Class: ConsoleWindowClass, Priority: X, HWND: X)
   ```

---

## ⚠️ ВОЗМОЖНЫЕ ПРИЧИНЫ

### 1. Окно ComfyUI не открыто
- **Решение:** Убедитесь, что ComfyUI запущен через `run_nvidia_gpu.bat`

### 2. Окно не видимо
- **Решение:** Окно должно быть видимым (не свернуто)

### 3. Окно имеет неожиданное название
- **Решение:** Проверьте логи - там будут показаны все найденные cmd.exe окна

### 4. Класс окна не ConsoleWindowClass
- **Решение:** Проверьте логи - там будет указан класс окна

### 5. Окно исключается из-за названия
- **Решение:** Если в названии есть исключенные ключевые слова, они будут показаны в логах

---

## 🛠️ РУЧНАЯ ПРОВЕРКА

Запустите скрипт для поиска всех окон:

```bash
python find_comfyui_window.py
```

Это покажет все окна с `comfy`, `python`, `qwen`, `cmd` в названии и их процессы.

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. **Перезапустите Window Waker**
2. **Проверьте логи** `logs/comfyui/waker.log`
3. **Найдите секцию** "Found X cmd.exe windows total"
4. **Проверьте**, какие окна найдены и почему они не подходят
5. **Сообщите результаты** - я скорректирую логику поиска

---

**Статус:** ✅ Добавлено детальное логирование для диагностики
