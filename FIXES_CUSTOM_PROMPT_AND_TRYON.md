# Исправления: Свой промпт и Примерочная

## Проблема 1: Порядок в разделе "Свой промпт" ✅ ИСПРАВЛЕНО

### Описание проблемы
Раздел "Свой промпт" работал в неправильном порядке:
1. Бот просил ввести промпт
2. Затем просил загрузить фото

Это создавало неудобство для пользователя, так как сначала нужно понять, какое фото будет обрабатываться.

### Требуемое поведение
Правильная последовательность:
1. Бот просит загрузить фото
2. Пользователь загружает фото
3. Бот просит подтвердить фото
4. После подтверждения бот просит ввести промпт
5. Пользователь вводит промпт
6. Фото отправляется на обработку

### Внесенные изменения

#### 1. `/bot/handlers/custom_prompt.py`
- **`start_custom_prompt()`**: Теперь сразу устанавливает состояние `UserState.awaiting_image_for_custom` и просит загрузить фото
- **`handle_custom_prompt()`**: Обрабатывает промпт ПОСЛЕ загрузки фото, проверяет наличие photo_id, создает задачу через API

#### 2. `/bot/handlers/image_upload.py`
- **`handle_image_upload_for_custom()`**: Сохраняет photo_id и просит подтвердить фото
- **`confirm_custom_photo()`**: Новый обработчик подтверждения фото, переключает состояние на `awaiting_custom_prompt`
- **`cancel_custom_photo()`**: Новый обработчик отмены загрузки фото
- **`custom_photo_confirmation_keyboard()`**: Новая клавиатура с кнопками "✅ Подтвердить" и "❌ Отменить"

### Новый flow
```
UserState.main_menu
    ↓ (нажатие "✍️ Свой промпт")
UserState.awaiting_image_for_custom
    ↓ (загрузка фото)
    ↓ (показ кнопок подтверждения)
    ↓ (нажатие "✅ Подтвердить")
UserState.awaiting_custom_prompt
    ↓ (ввод промпта)
    ↓ (создание задачи через API)
UserState.processing_job
```

---

## Проблема 2: Node IDs для фотографий в примерочной ✅ ИСПРАВЛЕНО

### Описание проблемы
Раздел "Примерочная" требует 2 фотографии:
1. Фото пользователя (человек, на которого примеряют одежду)
2. Фото одежды (товар с маркетплейса)

**Обнаруженные проблемы:**
1. Backend и Bot правильно передавали 2 фотографии через `second_image_file`
2. Worker НЕ ДЕСЕРИАЛИЗОВАЛ `second_image_path` из Redis при создании Job объекта
3. Workflow `qwen_edit_2511.py` поддерживал ТОЛЬКО 1 фотографию (нода 41)
4. Не было отдельного workflow для примерочной с 2 фотографиями

### Требуемое поведение
- Примерочная должна использовать специальный workflow с ДВУМЯ входными нодами LoadImage
- Worker должен правильно обрабатывать `second_image_path`
- Processor должен выбирать правильный workflow в зависимости от количества фотографий

### Внесенные изменения

#### 1. `/worker/main.py`
Исправлена десериализация Job из Redis:
```python
job = Job(
    id=job_data['id'],
    user_id=job_data['user_id'],
    image_path=job_data['image_path'],
    second_image_path=job_data.get('second_image_path'),  # ← ДОБАВЛЕНО
    prompt=job_data['prompt'],
    status=job_data['status'],
    created_at=parsed_created_at,
    updated_at=parsed_updated_at
)
```

#### 2. `/worker/workflows/qwen_tryon.py` (НОВЫЙ ФАЙЛ)
Создан новый workflow для примерочной с ДВУМЯ LoadImage нодами:

**Структура workflow:**
- **Нода 41**: LoadImage для первого фото (пользователь)
- **Нода 42**: LoadImage для второго фото (одежда)
- **Нода 79**: ImageScaleToTotalPixels для первого фото
- **Нода 80**: ImageScaleToTotalPixels для второго фото
- **Нода 68**: TextEncodeQwenImageEditPlus с `image1` и `image2` параметрами
- **Нода 69**: TextEncodeQwenImageEditPlus (negative) с `image1` и `image2` параметрами

**Ключевые отличия от стандартного workflow:**
1. Две входные LoadImage ноды (41 и 42)
2. Два ImageScaleToTotalPixels ноды (79 и 80)
3. TextEncodeQwenImageEditPlus принимает ОБА изображения (`image1` и `image2`)

#### 3. `/worker/processors/image_editor.py`
Обновлен processor для выбора правильного workflow:

```python
# Импорты
from worker.workflows.qwen_edit_2511 import build_workflow as build_single_workflow
from worker.workflows.qwen_tryon import build_tryon_workflow

# В методе process()
if job.second_image_path:
    # Try-on workflow (2 images)
    workflow = build_tryon_workflow(job)
else:
    # Standard workflow (1 image)
    workflow = build_single_workflow(job)
```

**Дополнительные улучшения:**
- Проверка существования второго изображения
- Логирование типа workflow (try-on или standard)
- Очистка обоих файлов после обработки

### Технические детали

#### Node ID структура

**Стандартный workflow (1 фото):**
- Нода 41: LoadImage (единственное изображение)
- Нода 79: ImageScaleToTotalPixels (scaling)
- Нода 68: TextEncodeQwenImageEditPlus с `image1` = нода 79

**Try-on workflow (2 фото):**
- Нода 41: LoadImage (первое фото - пользователь)
- Нода 42: LoadImage (второе фото - одежда)
- Нода 79: ImageScaleToTotalPixels (первое фото)
- Нода 80: ImageScaleToTotalPixels (второе фото)
- Нода 68: TextEncodeQwenImageEditPlus с `image1` = нода 79, `image2` = нода 80

#### Промпт для примерочной
Специальный промпт, который уже установлен в боте (menu.py, строки 912-923):
```
Use photo 1 as the primary subject reference.
Preserve the face, facial features, skin texture, head shape and overall identity from photo 1 exactly.
Use photo 2 as clothing reference only.
Take only the clothing item from photo 2.
Do not transfer the person, face, body shape, pose, hair or background from photo 2.
Dress the person from photo 1 in the clothing from photo 2.
Ensure the clothing fits naturally to the body proportions of the person from photo 1.
Maintain realistic fabric folds, texture, proportions and lighting.
Do not change the hairstyle, face, facial expression or body shape from photo 1.
Photorealistic result, high realism, natural lighting.
```

### Проверка корректности

#### Что проверить:
1. ✅ Backend API принимает `second_image_file` в `/api/jobs/create`
2. ✅ Job model в backend имеет поле `second_image_path`
3. ✅ Redis сохраняет `second_image_path` в job data
4. ✅ Worker десериализует `second_image_path` из Redis
5. ✅ Processor выбирает правильный workflow
6. ✅ Try-on workflow имеет 2 LoadImage ноды
7. ✅ Processor проверяет существование обоих файлов
8. ✅ Processor удаляет оба файла после обработки

#### Как протестировать:
1. Запустить бота
2. Выбрать "👕 ПРИМЕРОЧНАЯ"
3. Загрузить первое фото (пользователь)
4. Загрузить второе фото (одежда)
5. Дождаться результата
6. Проверить логи worker - должно быть сообщение "Building try-on workflow for job {id}"
7. Проверить результат - одежда должна быть примерена на пользователя

---

## Итоги

### Исправленные файлы:

#### Проблема 1 (Свой промпт):
1. `/bot/handlers/custom_prompt.py` - изменена последовательность
2. `/bot/handlers/image_upload.py` - добавлены обработчики подтверждения фото

#### Проблема 2 (Примерочная):
1. `/worker/main.py` - исправлена десериализация Job
2. `/worker/workflows/qwen_tryon.py` - создан новый try-on workflow
3. `/worker/processors/image_editor.py` - добавлен выбор workflow

### Новые файлы:
- `/worker/workflows/qwen_tryon.py` - workflow для примерочной с 2 фотографиями

### Состояния FSM (без изменений):
- `UserState.awaiting_image_for_custom` - ожидание фото для custom prompt
- `UserState.awaiting_custom_prompt` - ожидание промпта (после фото)
- `UserState.awaiting_first_fitting_photo` - ожидание первого фото для примерочной
- `UserState.awaiting_second_fitting_photo` - ожидание второго фото для примерочной

Все изменения обратно совместимы и не нарушают работу других функций бота.
